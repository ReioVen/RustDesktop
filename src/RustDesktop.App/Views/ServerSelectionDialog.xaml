<Window x:Class="RustDesktop.App.Views.ServerSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converters="clr-namespace:RustDesktop.App.Converters"
        mc:Ignorable="d"
        Title="Select Server" 
        Height="500" Width="600"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource MaterialDesignPaper}">
    
    <Window.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="Server" 
                                     VerticalAlignment="Center" 
                                     Margin="0,0,12,0"
                                     Foreground="{DynamicResource MaterialDesignPrimaryHueMidBrush}"
                                     Width="28"
                                     Height="28"/>
            <TextBlock Text="Multiple servers detected. Please select a server:" 
                       FontSize="18" 
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource MaterialDesignBody}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Server List -->
        <ListBox Grid.Row="1"
                 x:Name="ServerListBox"
                 SelectionMode="Single"
                 BorderThickness="0"
                 Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                            BorderThickness="0,0,0,1"
                            Padding="16,12"
                            Margin="0,0,0,4"
                            Background="{DynamicResource MaterialDesignCardBackground}"
                            CornerRadius="4"
                            materialDesign:ShadowAssist.ShadowDepth="Depth1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource MaterialDesignBody}"
                                               TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="{Binding Name}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Name}" Value="">
                                                        <Setter Property="Text" Value="Unknown Server"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Name}" Value="{x:Null}">
                                                        <Setter Property="Text" Value="Unknown Server"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock FontSize="14"
                                               Foreground="{DynamicResource MaterialDesignBody}"
                                               Margin="8,0,0,0"
                                               Opacity="0.7"
                                               VerticalAlignment="Center">
                                        <Run Text="("/>
                                        <Run Text="{Binding IpAddress}"/>
                                        <Run Text=":"/>
                                        <Run Text="{Binding Port}"/>
                                        <Run Text=")"/>
                                    </TextBlock>
                                </StackPanel>
                                <TextBlock FontSize="12"
                                           Foreground="{DynamicResource MaterialDesignBody}"
                                           Margin="0,4,0,0"
                                           Opacity="0.85">
                                    <Run Text="IP: "/>
                                    <Run Text="{Binding IpAddress}" FontWeight="SemiBold"/>
                                    <Run Text=" | Port: "/>
                                    <Run Text="{Binding Port}" FontWeight="SemiBold"/>
                                    <Run Text=" (Rust+ Port)"/>
                                </TextBlock>
                            </StackPanel>
                            <materialDesign:PackIcon Grid.Column="1"
                                                     Kind="CheckCircle"
                                                     VerticalAlignment="Center"
                                                     Foreground="{DynamicResource MaterialDesignPrimaryHueMidBrush}"
                                                     Width="24"
                                                     Height="24"
                                                     Visibility="Collapsed">
                                <materialDesign:PackIcon.Style>
                                    <Style TargetType="materialDesign:PackIcon">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </materialDesign:PackIcon.Style>
                            </materialDesign:PackIcon>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Padding="16,8"
                    Margin="0,0,12,0"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
            <Button Content="Connect"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Padding="16,8"
                    Click="ConnectButton_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
