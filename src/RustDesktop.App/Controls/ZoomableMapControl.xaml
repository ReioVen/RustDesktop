<UserControl x:Class="RustDesktop.App.Controls.ZoomableMapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#1E1E1E">
        <Grid x:Name="MapContainer" Margin="20">
            <Image x:Name="MapImage"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   MouseWheel="MapImage_MouseWheel"
                   MouseLeftButtonDown="MapImage_MouseLeftButtonDown"
                   MouseMove="MapImage_MouseMove"
                   MouseLeftButtonUp="MapImage_MouseLeftButtonUp">
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="ImageScaleTransform" 
                                       ScaleX="1" 
                                       ScaleY="1"/>
                        <TranslateTransform x:Name="ImageTranslateTransform"
                                           X="0"
                                           Y="0"/>
                    </TransformGroup>
                </Image.RenderTransform>
            </Image>
            
            <!-- Overlay for markers (vending machines, teammates) -->
            <Canvas x:Name="MarkersOverlay"
                    IsHitTestVisible="False"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Panel.ZIndex="1">
                <Canvas.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="OverlayScaleTransform" 
                                       ScaleX="1" 
                                       ScaleY="1"/>
                        <TranslateTransform x:Name="OverlayTranslateTransform"
                                           X="0"
                                           Y="0"/>
                    </TransformGroup>
                </Canvas.RenderTransform>
            </Canvas>
        </Grid>
    </Grid>
</UserControl>






